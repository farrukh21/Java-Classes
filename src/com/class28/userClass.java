package com.class28;

public class userClass {
	//Write program: userClass  that has a constructor that initializes name and mobile number instance variables. 
	//Create a subclass  userInfo that will have user address variable and it also being initialized through constructor call. Print users name,
	//mobile number and address in userDetails method. Test your code.
String name;
int mobileNum;
	userClass(String name, int mobileNum){
		this.name=name;
		this.mobileNum=mobileNum;
	}

}
